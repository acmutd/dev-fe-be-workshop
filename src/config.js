// WORKSHOP TODO (Task 3): You can customize these fallback values
// But the real customization happens in the backend API response!
export const FALLBACK_TITLE = "ACM Dashboard";
export const FALLBACK_ACCENT = "#7c3aed"; // Try: "#ef4444" (red), "#10b981" (green), "#f59e0b" (yellow)
export const AUTO_REFRESH_MS = 0; // Set to 5000 for auto-refresh every 5 seconds
